# このファイルを .env にコピーして値を設定する（.env は git 管理外）。
#   cp .env.example .env
# docker compose が同じディレクトリの .env を自動で読み込む。

# uvicorn のバインド先IP。
# 前段(別コンテナ)の Apache から届く docker ゲートウェイIP等を指定する。
#   例: 172.19.0.1
# 未設定なら 127.0.0.1（localhost のみ = LAN へ直接公開しない）
PI_INFO_HOST=127.0.0.1

# 待ち受けポート
PI_INFO_PORT=8001
